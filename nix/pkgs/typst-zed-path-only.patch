diff --git a/src/typst.rs b/src/typst.rs
--- a/src/typst.rs
+++ b/src/typst.rs
@@ -149,13 +149,18 @@ impl zed::Extension for TypstExtension {
         language_server_id: &zed::LanguageServerId,
         worktree: &zed::Worktree,
     ) -> Result<zed::Command> {
-        let tinymist_binary = self.language_server_binary(language_server_id, worktree)?;
+        let binary_args = LspSettings::for_worktree(language_server_id.as_ref(), worktree)
+            .ok()
+            .and_then(|settings| settings.binary)
+            .and_then(|binary| binary.arguments);
+        let tinymist_path = worktree.which("tinymist").ok_or_else(|| {
+            "`tinymist` was not found in PATH; install it declaratively and restart Gram"
+                .to_string()
+        })?;
-
+
         Ok(zed::Command {
-            command: tinymist_binary.path,
-            args: tinymist_binary
-                .args
-                .unwrap_or_else(|| vec!["lsp".to_string()]),
-            env: tinymist_binary.environment.unwrap_or_default(),
+            command: tinymist_path,
+            args: binary_args.unwrap_or_else(|| vec!["lsp".to_string()]),
+            env: worktree.shell_env(),
         })
     }
